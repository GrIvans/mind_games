import 'package:flutter/material.dart';

class StronglyDominatedStrategiesScreenWidget extends StatelessWidget {
  const StronglyDominatedStrategiesScreenWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Удаление строго доминируемых стратегий"),
      ),
    );
  }
}
